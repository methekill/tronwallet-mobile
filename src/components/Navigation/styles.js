import styled from 'styled-components'
import { TouchableOpacity } from 'react-native'

export const LeftBackButtonTouchable = styled(TouchableOpacity)`
  position: absolute;
  margin: 5px;
  left: 10;
`
